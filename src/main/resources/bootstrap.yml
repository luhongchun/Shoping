server:
  port: 8008
spring:
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&amp;characterEncoding=utf-8
    username: root
    password: 
    test-while-idle: true
    default-auto-commit: true
    max-idle: 30
    initial-size: 3
    connection-timeout: 30
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://test.mircoservice.smart-f.cn:8761/eureka/